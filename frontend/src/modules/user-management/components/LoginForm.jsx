function LoginForm() {
  return (
    <section className="profile-card">
      <span className="workspace-layout__eyebrow">User management</span>
      <h2>Login</h2>
      <p>Authentication is handled through environment-based demo identities in this workspace.</p>
    </section>
  );
}

export default LoginForm;
